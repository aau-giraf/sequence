<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="OverDraw"
    android:id="@+id/parent_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/giraf_background"
    android:focusableInTouchMode="true"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/noExistingSequencesWarning"
        android:layout_width="600dp"
        android:layout_height="30dp"
        android:layout_marginTop="200dp"
        android:layout_gravity="center_horizontal"
        android:gravity="center"
        android:text="@string/no_existing_sequences_warning"
        android:textColor="@android:color/darker_gray"
        android:textSize="22sp"/>
    <TextView
        android:id="@+id/noExistingSequencesHint"
        android:layout_width="600dp"
        android:layout_height="30dp"
        android:layout_gravity="center_horizontal"
        android:gravity="center"
        android:text="@string/no_existing_sequences_hint"
        android:textColor="@android:color/darker_gray"
        android:textSize="22sp"/>

    <GridView
        android:id="@+id/sequence_grid"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:numColumns="auto_fit"
        android:fadeScrollbars="false"
        android:listSelector="#00000000"/>
</LinearLayout>